
<h1>All the Pets</h1>

<%= link_to "I lost my pet :(", new_pet_path %>

<ul>
  <% @pets.each do |pet| %>
    <li>[<%= pet.category %>] - <%= link_to pet.name, pet_path(pet) %> found on <%= pet.found %></li>
  <% end %>
</ul>
